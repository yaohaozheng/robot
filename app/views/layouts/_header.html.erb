<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container-fluid">

    <div class="navbar-header">
      <%= link_to root_path, class: 'navbar-brand', style: "color:white" do %>
          <i class="fa fa-leaf" aria-hidden="true"></i> RailsChat
      <% end %>
    </div>

    <ul class="nav navbar-nav navbar-right">

      <% if current_user %>
          <% if current_user.admin? %>
            <li><%= link_to "All Users", users_path %></li>
          <% end %>

          <li>
            <%= link_to chats_path, class: "dropbtn" do %>
                ChatRoom  <i class="fa fa-wechat"></i>
            <% end %>
          </li>
          

          <!--<li>
            <%= link_to user_path(current_user), class: "dropbtn" do %>
                <%= current_user.name %> <span class="glyphicon glyphicon-user"></span>
            <% end %>
          </li> -->
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              <%= current_user.name %> 
              <span class="glyphicon glyphicon-user"></span><b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to "Profile", current_user %></li>
              <li><%= link_to "Settings", edit_user_path(current_user) %></li>
              <li class="divider"></li>
              <!--<li>
                <%= link_to "Log out", sessions_logout_path, method: :delete %>
              </li> -->
              <li>
                <%= link_to sessions_logout_path, method: :delete do %>
                    loggout <span class="	glyphicon glyphicon-off"></span>
                <% end %>
              </li>
            </ul> 
          </li>
      <% end %>
    </ul>
  </div>
  </div>
</nav>

<%= render 'layouts/login_panel' %>




